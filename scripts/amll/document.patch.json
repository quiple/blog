{
  "version": "0.6.0",
  "sha256": "fd3dd4a8ba727b7cbb860b0e0879683f18c74cf0116297d3b8239fcac6e6af5c",
  "changes": [
    {
      "label": "Document layout state",
      "before": "var LyricPlayerBase = class extends EventTarget {",
      "after": "const lineTiming = new WeakMap();\nvar LyricPlayerBase = class extends EventTarget {\n layoutVersion=0; hasStarted=false; interlude; interludeTop=0; layoutWidth=-1; documentLayoutReady=false;"
    },
    {
      "label": "Per-line render mode",
      "before": "var LyricLineBase = class extends EventTarget {",
      "after": "var LyricLineBase = class extends EventTarget {\n get isLineTimed(){ return lineTiming.get(this.getLine()) ?? false; }"
    },
    {
      "label": "First playback state",
      "before": "resume() {\n\t\tthis.interludeDots.resume();\n",
      "after": "resume() {\n this.hasStarted=true;\n\t\tthis.interludeDots.resume();\n"
    },
    {
      "label": "Annotated line timing: rebuildElement",
      "before": "\t\tif (this.lyricPlayer._getIsNonDynamic()) {\n",
      "after": "\t\tif ((this.isLineTimed && !this.lyricLine.words.some(word=>word.ruby?.length || word.romanWord?.trim()))) {\n"
    },
    {
      "label": "Annotated line timing: buildWord",
      "before": "\t\tlet emp = chunk.some((word) => LyricLineBase.shouldEmphasize(word));\n",
      "after": "\t\tlet emp = !this.isLineTimed && chunk.some((word) => LyricLineBase.shouldEmphasize(word));\n"
    },
    {
      "label": "Line language and translated color",
      "before": "\t\ttrans.setAttribute(\"class\", lyric_player_module_default.lyricSubLine);",
      "after": "\t\ttrans.setAttribute(\"class\", lyric_player_module_default.lyricSubLine);\n if(this.lyricLine.lang && this.lyricLine.lang!=='ko') main.setAttribute('lang',this.lyricLine.lang);\n if(this.lyricLine.pronunciationLang && this.lyricLine.pronunciationLang!=='ko') roman.setAttribute('lang',this.lyricLine.pronunciationLang);\n trans.classList.add('text-muted-foreground');\n for (const annotation of [roman, trans]) annotation.style.setProperty('--amll-annotation-opacity', this.isLineTimed ? '1' : 'var(--bright-mask-alpha, 0.4)');"
    },
    {
      "label": "Pronunciation before translation: constructor",
      "before": "\t\tthis.element.appendChild(document.createElement(\"div\"));\n\t\tconst main = this.element.children[0];\n\t\tconst trans = this.element.children[1];\n\t\tconst roman = this.element.children[2];",
      "after": "\t\tthis.element.appendChild(document.createElement(\"div\"));\n\t\tconst main = this.element.children[0];\n\t\tconst trans = this.element.children[2];\n\t\tconst roman = this.element.children[1];"
    },
    {
      "label": "Pronunciation before translation: rebuild",
      "before": "\trebuildElement() {\n\t\tthis.disposeElements();\n\t\tconst main = this.element.children[0];\n\t\tconst trans = this.element.children[1];\n\t\tconst roman = this.element.children[2];",
      "after": "\trebuildElement() {\n\t\tthis.disposeElements();\n\t\tconst main = this.element.children[0];\n\t\tconst trans = this.element.children[2];\n\t\tconst roman = this.element.children[1];"
    },
    {
      "label": "Pronunciation before translation: dispose",
      "before": "\t\tthis.splittedWords = [];\n\t\tconst main = this.element.children[0];\n\t\tconst trans = this.element.children[1];\n\t\tconst roman = this.element.children[2];",
      "after": "\t\tthis.splittedWords = [];\n\t\tconst main = this.element.children[0];\n\t\tconst trans = this.element.children[2];\n\t\tconst roman = this.element.children[1];"
    },
    {
      "label": "Muted pronunciation",
      "before": "\t\troman.setAttribute(\"class\", lyric_player_module_default.lyricSubLine);",
      "after": "\t\troman.setAttribute(\"class\", lyric_player_module_default.lyricSubLine);\n\t\troman.classList.add(\"text-muted-foreground\");"
    },
    {
      "label": "Input timing classification",
      "before": "\t\toptimizeLyricLines(this.processedLines, this.optimizeOpts);",
      "after": "\t\tfor(const line of this.processedLines) lineTiming.set(line, !line.wordTimed && line.words.every(word => word.startTime===line.startTime && word.endTime===line.endTime && (!word.ruby || word.ruby.every(part=>part.startTime===line.startTime && part.endTime===line.endTime))));\n\t\toptimizeLyricLines(this.processedLines, this.optimizeOpts);"
    },
    {
      "label": "Explicit word timing for single-word songs",
      "before": "if (line.words.length > 1) checkNonDynamic = false;",
      "after": "if (line.wordTimed || line.words.length > 1) checkNonDynamic = false;"
    },
    {
      "label": "Document coordinates using native layout calculator",
      "before": "\tcalcLayout(reason) {\n\t\tconst strategy = LayoutReasonStrategyMap[reason];\n\t\tconst snapshot = this.timelineController.getSnapshot();\n\t\tconst interlude = snapshot.activeInterlude;\n\t\tlet canDisplayInterlude = false;\n\t\tif (interlude) canDisplayInterlude = this.interludeDots.setInterlude([interlude.startTime, interlude.endTime], snapshot.currentTime, strategy.resetInterlude, interlude.anchorLineIndex);\n\t\telse this.interludeDots.clearInterlude();\n\t\tconst focalTarget = this.focusController.resolve(snapshot, this.currentLyricGroups.length, {\n\t\t\tisAutoAlignSuspended: this.scrollState.isAutoAlignSuspended,\n\t\t\thasBottomContent: this.hasBottomContent,\n\t\t\tcanDisplayInterlude\n\t\t});\n\t\tconst dotMargin = (this.baseFontSize || 24) * .4;\n\t\tconst totalInterludeHeight = this.layoutState.interludeDotsSize[1] + dotMargin * 2;\n\t\tconst ctx = this.frameContext;\n\t\tctx.containerHeight = this.size[1];\n\t\tctx.target = focalTarget;\n\t\tctx.bottomLineHeight = this.bottomLine.lineSize[1] || 0;\n\t\tconst activeInterludeForLayout = canDisplayInterlude ? interlude : void 0;\n\t\tconst interludeAnchorIndex = LayoutCalculator.resolveInterludeAnchorIndex(activeInterludeForLayout, focalTarget);\n\t\tif (interludeAnchorIndex !== void 0) {\n\t\t\tctx.interlude = ctx.interlude || {\n\t\t\t\ttotalHeight: 0,\n\t\t\t\tanchorIndex: 0\n\t\t\t};\n\t\t\tctx.interlude.totalHeight = totalInterludeHeight;\n\t\t\tctx.interlude.anchorIndex = interludeAnchorIndex;\n\t\t} else ctx.interlude = void 0;\n\t\tconst { bounds, session } = this.layoutCalculator.beginFrame(ctx, this.layoutConfig);\n\t\tctx.scrollOffset = this.scrollEngine.updateBoundary(bounds.min, bounds.max);\n\t\tconst result = this.layoutCalculator.commit(session, ctx.scrollOffset);\n\t\tif (result.hasInterlude && canDisplayInterlude && interlude) {\n\t\t\tconst nextLineIndex = interlude.anchorLineIndex + 1;\n\t\t\tconst targetX = this.currentLyricGroups[nextLineIndex]?.mainLine.getLine().isDuet ?? false ? this.size[0] - this.layoutState.interludeDotsSize[0] : 0;\n\t\t\tthis.interludeDots.setTransform(targetX, result.interludeY + dotMargin, strategy.snapPosY || !this.getEnableSpring());\n\t\t}\n\t\tconst visual = this.visualFrame;\n\t\tvisual.latestIndex = snapshot.latestHighlightedIndex ?? snapshot.scrollToIndex;\n\t\tvisual.passedBoundary = interlude ? interlude.anchorLineIndex + 1 : snapshot.scrollToIndex;\n\t\tvisual.isNarrowViewport = window.innerWidth <= 1024;\n\t\tconst activeCount = result.lineCount;\n\t\tlet delay = Duration.ZERO;\n\t\tlet baseDelay = strategy.disableStagger ? Duration.ZERO : Duration.fromSecs(.05);\n\t\tfor (let i = 0; i < activeCount; i++) {\n\t\t\tconst group = this.currentLyricGroups[i];\n\t\t\tconst instruction = result.lineInstructions[i];\n\t\t\tconst curPos = instruction.y;\n\t\t\tconst isInViewport = instruction.isInViewport;\n\t\t\tconst isActive = this.resolveIsActive(i, snapshot);\n\t\t\tgroup.setTransform(curPos, strategy.snapPosY, delay, isActive, this.resolveOpacity(i, isInViewport, snapshot), this.resolveBlurLevel(i, isActive, isInViewport, snapshot));\n\t\t\tif (curPos + instruction.height >= 0) {\n\t\t\t\tdelay = Duration.add(delay, baseDelay);\n\t\t\t\tif (i >= snapshot.scrollToIndex) baseDelay = Duration.mulF64(baseDelay, 1 / 1.05);\n\t\t\t}\n\t\t}\n\t\tconst isBottomFocused = focalTarget.type === \"bottom\";\n\t\tthis.bottomLine.setFocused(isBottomFocused);\n\t\tthis.bottomLine.setTransform(result.bottomLineY, this.resolveBlurLevel(activeCount, isBottomFocused, result.isBottomLineInViewport, snapshot), strategy.snapPosY, delay);\n\t}",
      "after": "\tcalcLayout(reason = LayoutReason.ConfigChange) {\n\t\tconst width = this.element.clientWidth;\n        const initial = !this.documentLayoutReady || this.layoutWidth !== width;\n        this.documentLayoutReady = true; this.layoutWidth = width;\n        const strategy = initial ? LayoutReasonStrategyMap[LayoutReason.RebuildView] : LayoutReasonStrategyMap[reason];\n\t\tconst snapshot = this.timelineController.getSnapshot();\n\t\tconst interlude = this.hasStarted ? snapshot.activeInterlude : undefined;\n        this.interlude = interlude;\n\t\tlet canDisplayInterlude = false;\n\t\tif (interlude) canDisplayInterlude = this.interludeDots.setInterlude([interlude.startTime, interlude.endTime], snapshot.currentTime, strategy.resetInterlude, interlude.anchorLineIndex);\n\t\telse this.interludeDots.clearInterlude();\n\t\tconst focalTarget = this.focusController.resolve(snapshot, this.currentLyricGroups.length, {\n\t\t\tisAutoAlignSuspended: this.scrollState.isAutoAlignSuspended,\n\t\t\thasBottomContent: this.hasBottomContent,\n\t\t\tcanDisplayInterlude\n\t\t});\n\t\tconst dotMargin = (this.baseFontSize || 24) * .4;\n\t\tconst totalInterludeHeight = this.layoutState.interludeDotsSize[1] + dotMargin * 2;\n\t\tconst ctx = this.frameContext;\n\t\tctx.containerHeight = this.size[1];\n\t\tctx.target = focalTarget;\n\t\tctx.bottomLineHeight = this.bottomLine.lineSize[1] || 0;\n\t\tconst activeInterludeForLayout = canDisplayInterlude ? interlude : void 0;\n\t\tconst interludeAnchorIndex = LayoutCalculator.resolveInterludeAnchorIndex(activeInterludeForLayout, focalTarget);\n\t\tif (interludeAnchorIndex !== void 0) {\n\t\t\tctx.interlude = ctx.interlude || {\n\t\t\t\ttotalHeight: 0,\n\t\t\t\tanchorIndex: 0\n\t\t\t};\n\t\t\tctx.interlude.totalHeight = totalInterludeHeight;\n\t\t\tctx.interlude.anchorIndex = interludeAnchorIndex;\n\t\t} else ctx.interlude = void 0;\n\t\tconst { bounds, session } = this.layoutCalculator.beginFrame(ctx, this.layoutConfig);\n\t\tctx.scrollOffset = this.scrollEngine.updateBoundary(bounds.min, bounds.max);\n\t\tconst result = this.layoutCalculator.commit(session, ctx.scrollOffset);\n        const origin = (result.lineInstructions[0]?.y ?? 0) - (result.hasInterlude && interlude?.anchorLineIndex === -1 ? totalInterludeHeight : 0);\n\t\tif (result.hasInterlude && canDisplayInterlude && interlude) {\n\t\t\tconst nextLineIndex = interlude.anchorLineIndex + 1;\n\t\t\tconst targetX = this.currentLyricGroups[nextLineIndex]?.mainLine.getLine().isDuet ?? false ? this.size[0] - this.layoutState.interludeDotsSize[0] : 0;\n\t\t\tthis.interludeTop = result.interludeY + dotMargin - origin;\n            this.interludeDots.setTransform(targetX, this.interludeTop, strategy.snapPosY || !this.getEnableSpring());\n\t\t}\n\t\tconst visual = this.visualFrame;\n\t\tvisual.latestIndex = snapshot.latestHighlightedIndex ?? snapshot.scrollToIndex;\n\t\tvisual.passedBoundary = interlude ? interlude.anchorLineIndex + 1 : snapshot.scrollToIndex;\n\t\tvisual.isNarrowViewport = window.innerWidth <= 1024;\n\t\tconst activeCount = result.lineCount;\n\t\tlet delay = Duration.ZERO;\n\t\tlet baseDelay = strategy.disableStagger ? Duration.ZERO : Duration.fromSecs(.05);\n\t\tfor (let i = 0; i < activeCount; i++) {\n\t\t\tconst group = this.currentLyricGroups[i];\n\t\t\tconst instruction = result.lineInstructions[i];\n\t\t\tconst curPos = instruction.y;\n\t\t\tconst isInViewport = true;\n\t\t\tconst isActive = this.hasStarted && this.resolveIsActive(i, snapshot);\n\t\t\tgroup.setTransform(curPos - origin, strategy.snapPosY, delay, isActive, this.resolveOpacity(i, isInViewport, snapshot), this.resolveBlurLevel(i, isActive, isInViewport, snapshot));\n\t\t\tif (curPos + instruction.height >= 0) {\n\t\t\t\tdelay = Duration.add(delay, baseDelay);\n\t\t\t\tif (i >= snapshot.scrollToIndex) baseDelay = Duration.mulF64(baseDelay, 1 / 1.05);\n\t\t\t}\n\t\t}\n        const last = result.lineInstructions[activeCount - 1];\n        this.element.style.height = Math.max(0, last ? last.y + last.height - origin : 0) + 'px';\n        this.layoutVersion++;\n\t\tconst isBottomFocused = focalTarget.type === \"bottom\";\n\t\tthis.bottomLine.setFocused(isBottomFocused);\n\t\tthis.bottomLine.setTransform(result.bottomLineY, this.resolveBlurLevel(activeCount, isBottomFocused, result.isBottomLineInViewport, snapshot), strategy.snapPosY, delay);\n\t}"
    },
    {
      "label": "Document opacity per line",
      "before": "if (snapshot.highlightedGroups.has(index)) return .85;\n\t\treturn this.isNonDynamic ? .2 : 1;",
      "after": "if (this.hasStarted && snapshot.highlightedGroups.has(index)) return .85;\n        const group = this.currentLyricGroups[index];\n        const lineTimed = group.mainLine.isLineTimed && (!group.bgLine || group.bgLine.isLineTimed);\n        return lineTimed ? .2 : 1;"
    },
    {
      "label": "Keep document rows mounted",
      "before": "\tisInRenderRange(includeOverscan = true) {\n\t\tconst top = this.posY.getCurrentPosition();\n\t\tconst index = this.lyricPlayer.currentLyricGroups.indexOf(this);\n\t\tconst height = index !== -1 ? this.lyricPlayer.getLineHeight(index) : this.lyricPlayer.defaultLineHeight;\n\t\tconst viewportHeight = this.lyricPlayer.size[1];\n\t\tif (viewportHeight <= 0 || height <= 0) return false;\n\t\tconst overscan = includeOverscan ? this.lyricPlayer.getOverscanPx() : 0;\n\t\treturn top < viewportHeight + overscan && top + height > -overscan;\n\t}",
      "after": "\tisInRenderRange(){return true;}"
    },
    {
      "label": "Measure document rows",
      "before": "var DomLyricPlayer = class extends LyricPlayerBase {",
      "after": "var DomLyricPlayer = class extends LyricPlayerBase {\n    calcLayout(reason = LayoutReason.ConfigChange) {\n        this.currentLyricGroups.forEach((group, index) => {\n            group.show();\n            if (!this.lyricGroupSize.has(group)) this.lyricGroupSize.set(group, [group.element.clientWidth, group.element.clientHeight]);\n            this.layoutCalculator.setLineHeight(index, this.lyricGroupSize.get(group)[1]);\n        });\n        return super.calcLayout(reason);\n    }"
    },
    {
      "label": "Use browser document scroll events",
      "before": "\t\tthis.scrollEngine = new ScrollInteractionEngine(this.element, {\n\t\t\tonScrollUpdate: (isContinuous) => {\n\t\t\t\tthis.calcLayout(isContinuous ? LayoutReason.ContinuousScroll : LayoutReason.DiscreteScroll);\n\t\t\t},\n\t\t\tonInteractionStart: (type) => {\n\t\t\t\tthis.scrollState.isAutoAlignSuspended = true;\n\t\t\t\tthis.scrollState.isTouchScrolled = type === \"touch\";\n\t\t\t\tthis.calcLayout(LayoutReason.InteractionStart);\n\t\t\t}\n\t\t});",
      "after": "\t\tthis.scrollEngine = new ScrollInteractionEngine(this.element, {\n\t\t\tonScrollUpdate: (isContinuous) => {\n\t\t\t\tthis.calcLayout(isContinuous ? LayoutReason.ContinuousScroll : LayoutReason.DiscreteScroll);\n\t\t\t},\n\t\t\tonInteractionStart: (type) => {\n\t\t\t\tthis.scrollState.isAutoAlignSuspended = true;\n\t\t\t\tthis.scrollState.isTouchScrolled = type === \"touch\";\n\t\t\t\tthis.calcLayout(LayoutReason.InteractionStart);\n\t\t\t}\n\t\t});\n        this.scrollEngine.dispose();"
    },
    {
      "label": "Annotated line timing: updateMaskImageSync",
      "before": "\t\tif (this.balancer && LyricLineBase.wordSegmenter) this.balancer.balanceLineBreaks(this.lyricPlayer._getIsNonDynamic(), this.splittedWords.length > 0, LyricLineBase.wordSegmenter);",
      "after": "\t\tif (this.balancer && LyricLineBase.wordSegmenter) this.balancer.balanceLineBreaks((this.isLineTimed && !this.lyricLine.words.some(word=>word.ruby?.length || word.romanWord?.trim())), this.splittedWords.length > 0, LyricLineBase.wordSegmenter);\n        if(this.isLineTimed) return;"
    },
    {
      "label": "Line-timed word animation",
      "before": "elementAnimations: [createFloatAnimation(mainWordEl, {",
      "after": "elementAnimations: this.isLineTimed ? [] : [createFloatAnimation(mainWordEl, {"
    },
    {
      "label": "Ruby spacing",
      "before": "rubyPartEl.textContent = ruby.word;",
      "after": "for(const {segment} of LyricLineBase.graphemeSegmenter?.segment(ruby.word) ?? Array.from(ruby.word,segment=>({segment}))){const glyph=document.createElement('span');glyph.textContent=segment;rubyPartEl.appendChild(glyph);}"
    },
    {
      "label": "Pronunciation language",
      "before": "romanWordEl.classList.add(lyric_player_module_default.romanWord);",
      "after": "romanWordEl.classList.add(lyric_player_module_default.romanWord);\n            romanWordEl.lang = \"ko\";"
    },
    {
      "label": "Preserve annotated input chunks",
      "before": "\t\tif (hasRuby) {\n\t\t\tconst leadingSpaceMatch",
      "after": "\t\tif (hasRuby || romanWord.trim()) {\n\t\t\tconst leadingSpaceMatch"
    },
    {
      "label": "Dots follow animated row edges",
      "before": "\t\tfor (const group of this.currentLyricGroups) group.commitChanges();",
      "after": "\t\tfor (const group of this.currentLyricGroups) group.commitChanges();\n        // The document does not move with the native viewport. Position the dots\n        // between the rendered row edges, while their springs open the gap.\n        const dots = this.interludeDots.getElement();\n        if (this.interlude) {\n            const index = this.interlude.anchorLineIndex;\n            const before = this.currentLyricGroups[index];\n            const after = this.currentLyricGroups[index + 1];\n            if (after) {\n                const bottom = before ? before.posY.getCurrentPosition() + (this.lyricGroupSize.get(before)?.[1] ?? 0) : 0;\n                const top = after.posY.getCurrentPosition();\n                const height = this.layoutState.interludeDotsSize[1];\n                dots.style.translate = `0 ${(top + bottom - height) / 2 - this.interludeDots.posY.getCurrentPosition()}px`;\n                dots.style.visibility = top - bottom + 1 < height + (this.baseFontSize || 24) * 0.8 ? 'hidden' : '';\n            }\n        }\n        else {\n            dots.style.translate = '';\n            dots.style.visibility = '';\n        }"
    }
  ]
}
